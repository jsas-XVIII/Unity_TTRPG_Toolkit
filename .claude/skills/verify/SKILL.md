---
name: verify
description: Run tests, lint, and report status
---
Run `npm test` then `npm run lint`. Report:
1. Test count and pass/fail
2. Lint status
3. If both pass, list files changed since last commit and propose a commit message (wait for approval before committing).
