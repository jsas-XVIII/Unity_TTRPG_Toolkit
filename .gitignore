# Local reference files (copyrighted material / local dev only)
Unity - Core Rules.pdf
unity_rules.txt

# GitLab runner state
.runner_system_id

# Local Claude Code settings (may contain tokens or machine-specific paths)
.claude/settings.local.json
